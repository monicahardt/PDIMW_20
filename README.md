# The University Effect

Advanced / Reflective UI prototype for a personal data interaction dashboard.

## Run locally

```bash
npm install
npm run dev
```

The two Observable chart slots live in `src/main.tsx` as reusable `ObservableSection`
and `ObservableEmbed` components. Add an Observable iframe URL through the optional
`src` prop when the real charts are ready.
